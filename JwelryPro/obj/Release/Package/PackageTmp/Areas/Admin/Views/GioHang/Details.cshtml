@model IEnumerable<JwelryPro.Models.DETAIL_CARDS>

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.QUANTITY)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TOTAL_PRICE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ID_CARDS)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ID_PRO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.QUANTITY)
        </td>
        <td>

            @Html.DisplayFor(modelItem => item.TOTAL_PRICE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ID_CARDS)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ID_PRO)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditDetail", new { id=item.ID_DETAIL_CARDS }) @*|*@
            
            @*@Html.ActionLink("Delete", "DeleteDetail", new { id=item.ID_DETAIL_CARDS })*@
        </td>
    </tr>
}

</table>
